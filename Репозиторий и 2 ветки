git checkout feature-1
echo "Line 2: Added by feature-1" >> file.txt
git add file.txt
git commit -m "Add second line in feature-1"

git checkout feature-2
echo "Line 2: Added by feature-2 (different text)" >> file.txt
git add file.txt
git commit -m "Add second line in feature-2 with different text"

git log --oneline --graph --all
